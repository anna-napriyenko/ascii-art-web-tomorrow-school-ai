# ASCII Art Web

## Описание

**ASCII Art Web** — это простое веб-приложение, написанное на Go, которое преобразует введенный пользователем текст в ASCII-арт. Приложение использует кастомный веб-сервер, который обрабатывает GET и POST-запросы и рендерит HTML-шаблоны и CSS.

Проект поддерживает три различных стиля (баннера) для генерации арта:

  * `standard`
  * `shadow`
  * `thinkertoy`

## 💎 Особенности

  * **Веб-интерфейс:** Простой и понятный интерфейс для ввода текста и выбора стиля.
  * **Кастомный сервер:** Использует собственный экземпляр `http.Server` и `http.ServeMux` вместо глобальных обработчиков, что обеспечивает лучший контроль.
  * **Обработка ошибок:** Корректно обрабатывает различные типы ошибок, возвращая пользователю понятные страницы:
      * `400 Bad Request` (неверный ввод или не-ASCII символы)
      * `404 Not Found` (несуществующий URL)
      * `405 Method Not Allowed` (неверный метод HTTP)
      * `500 Internal Server Error` (проблемы на стороне сервера, например, отсутствие файла баннера)
  * **Чистая архитектура:** Логика разделена на пакеты (`files` для загрузки баннеров, `printing` для констант и `app` для веб-сервера).

## ⚙️ Требования

  * **Go:** Версия 1.25.0 или новее.

## 🚀 Запуск проекта

1.  Убедитесь, что у вас есть все файлы проекта, сохранив структуру директорий (особенно важны `app`, `banners`, `files`, `printing`).
2.  Перейдите в директорию приложения:
    ```bash
    cd ascii-art-web-stylize/app
    ```
3.  Запустите веб-сервер:
    ```bash
    go run .
    ```
4.  Сервер будет запущен и доступен по адресу `http://localhost:8080`.

## 📋 Как использовать

### 1\. Через браузер

  * Откройте `http://localhost:8080` в вашем браузере.
  * Введите любой текст (поддерживаются только ASCII символы) в текстовое поле.
  * Выберите один из трех стилей (`standard`, `shadow`, `thinkertoy`) из выпадающего меню.
  * Нажмите кнопку "Generate".
  * Ваш ASCII-арт появится в блоке "Result" в нижней части страницы.

### 2\. Через cURL (или Postman)

Вы можете отправлять запросы напрямую к эндпоинту `/ascii` с помощью `curl` или любого API-клиента, например, Postman.

  * **Эндпоинт:** `POST /ascii`
  * **Тип тела:** `x-www-form-urlencoded`
  * **Параметры:**
      * `text` (string): Текст для преобразования.
      * `banner` (string): Один из стилей: `standard`, `shadow` или `thinkertoy`.

**Пример запроса `curl`:**

```bash
curl -X POST http://localhost:8080/ascii \
     -d "text=Hello World" \
     -d "banner=shadow"
```
